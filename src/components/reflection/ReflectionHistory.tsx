import { Reflection } from '../../types';import Card from '../ui/Card';interface ReflectionHistoryProps {reflections: Reflection[];}export default function ReflectionHistory({ reflections }: ReflectionHistoryProps) {if (reflections.length === 0) {return (<Card className="cursor-default"><p className="text-text-secondary text-center">No reflections yet. Start reflecting daily!</p></Card>);}return (<div className="space-y-4">{reflections.map((r) => (<Card key={r.id} className="cursor-default"><div className="flex justify-between items-center mb-2"><span className="text-sm font-semibold text-text-primary">{new Date(r.date).toLocaleDateString()}</span><span className="text-xl" aria-label={r.mood}>{r.mood === 'happy' ? '😊' : r.mood === 'neutral' ? '😐' : '😞'}</span></div><p className="text-text-secondary text-sm whitespace-pre-wrap">{r.notes}</p></Card>))}</div>);} 