import { NavLink } from 'react-router-dom';import { Home, Calendar, CheckSquare, Clock, Target, BarChart3, Edit3, User } from 'lucide-react';import { useAppContext } from '../../context/AppContext';export default function Sidebar() {const { user, logout } = useAppContext();const navItems = [{to:'/',label:'LayoutDashboard',icon:Home},{to:'/planner',label:'Planner',icon:Calendar},{to:'/habits',label:'Habits',icon:CheckSquare},{to:'/focus-timer',label:'Focus Timer',icon:Clock},{to:'/goals',label:'Goals',icon:Target},{to:'/analytics',label:'Analytics',icon:BarChart3},{to:'/reflection',label:'Reflection',icon:Edit3},{to:'/profile',label:'UserRound',icon:User}];return (<aside className="fixed left-0 top-0 h-screen w-60 bg-surface-1 border-r border-border flex flex-col"><div className="h-16 px-6 flex items-center text-xl font-bold text-primary">TrackForge</div><nav className="flex-1 px-3 py-4 flex flex-col gap-1">{navItems.map(({ to, label, icon: Icon }) => (<NavLink key={to} to={to} end className={({ isActive }) => `h-10 px-3 rounded-lg flex items-center gap-3 text-sm transition-colors duration-150 ${isActive ? 'bg-primary-subtle text-primary font-medium' : 'text-text-secondary hover:text-text-primary hover:bg-surface-2'}`}><Icon className="w-5 h-5" />{label}</NavLink>))}</nav><div className="border-t border-border p-4 flex items-center gap-3"><img src={user?.avatar} alt={user?.name} className="w-10 h-10 rounded-full object-cover" /><div className="flex flex-col"><span className="text-sm font-medium text-text-primary">{user?.name}</span><button onClick={logout} className="text-xs text-danger hover:underline focus:outline-none">LogOut</button></div></div></aside>);} 