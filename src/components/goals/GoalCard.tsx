import { Goal } from '../../types';import ProgressBar from '../ui/ProgressBar';interface GoalCardProps {goal: Goal;onToggleComplete: (id: string) => void;}export default function GoalCard({ goal, onToggleComplete }: GoalCardProps) {return (<div className="p-4 rounded-xl border border-border shadow-md bg-surface-1 hover:shadow-xl hover:-translate-y-1 transition-all duration-300 cursor-pointer" onClick={() => onToggleComplete(goal.id)}><h4 className={`text-base font-semibold ${goal.completed ? 'line-through text-text-tertiary' : 'text-text-primary'}`}>{goal.title}</h4><p className="text-xs text-text-secondary mb-2">Target: {new Date(goal.targetDate).toLocaleDateString()}</p><ProgressBar progress={goal.progress} /></div>);} 