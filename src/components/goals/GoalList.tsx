import React, { useState } from 'react';import { Goal } from '../../types';import GoalCard from './GoalCard';import Input from '../ui/Input';interface GoalListProps {goals: Goal[];setGoals: React.Dispatch<React.SetStateAction<Goal[]>>;}export default function GoalList({ goals, setGoals }: GoalListProps) {const [search, setSearch] = useState('');const filteredGoals = goals.filter((g) => g.title.toLowerCase().includes(search.toLowerCase()));function toggleComplete(id: string) {setGoals((prev) => prev.map((g) => (g.id === id ? { ...g, completed: !g.completed } : g)));}return (<div className="space-y-4"><Input label="Search Goals" id="search-goals" placeholder="Search goals..." value={search} onChange={(e) => setSearch(e.target.value)} /><div className="grid grid-cols-1 sm:grid-cols-2 gap-4">{filteredGoals.length === 0 ? (<p className="text-text-secondary text-center col-span-full">No goals found.</p>) : filteredGoals.map((goal) => (<GoalCard key={goal.id} goal={goal} onToggleComplete={toggleComplete} />))}</div></div>);} 