import { Task } from '../../types';import { CheckCircle, Circle } from 'lucide-react';import Badge from '../ui/Badge';interface TaskCardProps {task: Task;onToggleComplete: (id: string) => void;}export default function TaskCard({ task, onToggleComplete }: TaskCardProps) {return (<div className="flex items-center justify-between p-4 rounded-xl border border-border shadow-md bg-surface-1 hover:shadow-xl hover:-translate-y-1 transition-all duration-300 cursor-pointer" onClick={() => onToggleComplete(task.id)}><div className="flex items-center gap-3"><button aria-label={task.completed ? 'Mark incomplete' : 'Mark complete'}>{task.completed ? <CheckCircle className="w-6 h-6 text-success" /> : <Circle className="w-6 h-6 text-text-tertiary" />}</button><div><h4 className={`text-base font-semibold ${task.completed ? 'line-through text-text-tertiary' : 'text-text-primary'}`}>{task.title}</h4><p className="text-xs text-text-secondary">{task.category} • {task.priority}</p></div></div><Badge variant={task.priority === 'High' ? 'danger' : task.priority === 'Medium' ? 'warning' : 'neutral'}>{task.priority}</Badge></div>);} 