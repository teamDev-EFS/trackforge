import React, { useState } from 'react';import { Task } from '../../types';import TaskCard from './TaskCard';import Input from '../ui/Input';interface TaskListProps {tasks: Task[];setTasks: React.Dispatch<React.SetStateAction<Task[]>>;}export default function TaskList({ tasks, setTasks }: TaskListProps) {const [search, setSearch] = useState('');const filteredTasks = tasks.filter((task) => task.title.toLowerCase().includes(search.toLowerCase()));function toggleComplete(id: string) {setTasks((prev) => prev.map((t) => (t.id === id ? { ...t, completed: !t.completed } : t)));}return (<div className="space-y-4"><Input label="Search Tasks" id="search-tasks" placeholder="Search tasks..." value={search} onChange={(e) => setSearch(e.target.value)} /><div className="grid grid-cols-1 sm:grid-cols-2 gap-4">{filteredTasks.length === 0 ? (<p className="text-text-secondary text-center col-span-full">No tasks found.</p>) : filteredTasks.map((task) => (<TaskCard key={task.id} task={task} onToggleComplete={toggleComplete} />))}</div></div>);} 