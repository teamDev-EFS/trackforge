interface CategoryFilterProps {categories: string[];selected: string;onSelect: (category: string) => void;}export default function CategoryFilter({ categories, selected, onSelect }: CategoryFilterProps) {return (<div className="flex gap-3"><button className={`px-3 py-1 rounded-lg text-sm font-medium ${selected === 'All' ? 'bg-primary text-white' : 'bg-surface-2 text-text-secondary hover:bg-surface-3 hover:text-text-primary'}`} onClick={() => onSelect('All')}>All</button>{categories.map((cat) => (<button key={cat} className={`px-3 py-1 rounded-lg text-sm font-medium ${selected === cat ? 'bg-primary text-white' : 'bg-surface-2 text-text-secondary hover:bg-surface-3 hover:text-text-primary'}`} onClick={() => onSelect(cat)}>{cat}</button>))}</div>);} 