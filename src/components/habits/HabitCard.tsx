import { Habit } from '../../types';import Badge from '../ui/Badge';interface HabitCardProps {habit: Habit;onToggleComplete: (id: string) => void;}export default function HabitCard({ habit, onToggleComplete }: HabitCardProps) {return (<div className="flex items-center justify-between p-4 rounded-xl border border-border shadow-md bg-surface-1 hover:shadow-xl hover:-translate-y-1 transition-all duration-300 cursor-pointer" onClick={() => onToggleComplete(habit.id)}><div><h4 className="text-base font-semibold text-text-primary">{habit.name}</h4><div className="flex gap-2 mt-1 text-xs text-text-secondary"><span>Current Streak: {habit.currentStreak}d</span><span>Longest: {habit.longestStreak}d</span><span>Weekly: {habit.weeklyCompletion}%</span></div></div><Badge variant={habit.completedToday ? 'success' : 'neutral'}>{habit.completedToday ? 'Done' : 'Pending'}</Badge></div>);} 