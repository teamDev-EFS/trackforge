import React, { useState } from 'react';import Modal from '../ui/Modal';import Input from '../ui/Input';import Button from '../ui/Button';import { Habit } from '../../types';interface AddHabitModalProps {isOpen: boolean;onClose: () => void;onAddHabit: (habit: Habit) => void;}export default function AddHabitModal({ isOpen, onClose, onAddHabit }: AddHabitModalProps) {const [name, setName] = useState('');const [error, setError] = useState('');function handleSubmit(e: React.FormEvent) {e.preventDefault();if (!name.trim()) {setError('Name is required');return;}const newHabit: Habit = {id: crypto.randomUUID(),name:name.trim(),currentStreak:0,longestStreak:0,weeklyCompletion:0,completedToday:false};onAddHabit(newHabit);setName('');setError('');onClose();}return (<Modal isOpen={isOpen} onClose={onClose} title="Add New Habit"><form onSubmit={handleSubmit} className="space-y-4"><Input label="Habit Name" id="habit-name" value={name} onChange={(e) => setName(e.target.value)} placeholder="Habit name" />{error && <p className="text-red-500 text-sm">{error}</p>}<div className="flex justify-end"><Button type="submit">Add Habit</Button></div></form></Modal>);} 