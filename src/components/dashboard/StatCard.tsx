import { Lucide } from 'lucide-react';import Badge from '../ui/Badge';interface StatCardProps {label: string;value: string | number;icon: Lucide;trend?: number;}export default function StatCard({ label, value, icon: Icon, trend }: StatCardProps) {const trendPositive = trend !== undefined && trend >= 0;return (<div className="rounded-xl border border-border shadow-md bg-surface-1 p-6 flex items-center gap-4"><div className="p-3 rounded-xl bg-primary/10 text-primary"><Icon className="w-6 h-6" /></div><div className="flex flex-col"><span className="text-sm text-text-secondary">{label}</span><span className="text-3xl font-bold text-text-primary">{value}</span>{trend !== undefined && (<Badge variant={trendPositive ? 'success' : 'error'}>{trendPositive ? '↑' : '↓'} {Math.abs(trend)}%</Badge>)}</div></div>);} 