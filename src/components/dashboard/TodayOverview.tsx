import { tasks } from '../../data/tasks';import Card from '../ui/Card';import { CheckCircle } from 'lucide-react';export default function TodayOverview() {const completedTasks = tasks.filter((t) => t.completed).length;const totalTasks = tasks.length;return (<Card className="cursor-default"><h3 className="text-lg font-semibold mb-4">Today's Overview</h3><p className="text-sm text-text-secondary mb-2">Tasks Completed</p><div className="flex items-center gap-2"><CheckCircle className="w-6 h-6 text-success" /><span className="text-2xl font-bold">{completedTasks} / {totalTasks}</span></div></Card>);} 