import Button from '../ui/Button';import { Plus } from 'lucide-react';import { useNavigate } from 'react-router-dom';export default function QuickActions() {const navigate = useNavigate();return (<div className="flex gap-4"><Button icon={Plus} onClick={() => navigate('/planner')}>Add Task</Button><Button variant="secondary" icon={Plus} onClick={() => navigate('/habits')}>Add Habit</Button></div>);} 