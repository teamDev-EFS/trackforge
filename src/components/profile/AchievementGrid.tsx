import { Achievement } from '../../types';import Card from '../ui/Card';import { Star, CheckCircle, Clock, type LucideIcon } from 'lucide-react';const iconMap: Record<string, LucideIcon> = { Star, CheckCircle, Clock };interface AchievementGridProps {achievements: Achievement[];}export default function AchievementGrid({ achievements }: AchievementGridProps) {if (achievements.length === 0) {return (<p className="text-text-secondary text-center">No achievements yet.</p>);}return (<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">{achievements.map((a) => {const Icon = iconMap[a.icon] ?? Star;return (<Card key={a.id} className="cursor-default"><div className="flex items-center gap-3"><Icon className="w-6 h-6 text-primary" /><div><h4 className="text-base font-semibold text-text-primary">{a.title}</h4><p className="text-xs text-text-secondary">{a.description}</p><p className="text-xs text-text-tertiary">{new Date(a.date).toLocaleDateString()}</p></div></div></Card>);})}</div>);} 