import { useAppContext } from '../../context/AppContext';export default function ProfileHeader() {const { user } = useAppContext();return (<div className="flex items-center gap-4"><img src={user?.avatar} alt={user?.name} className="w-20 h-20 rounded-full object-cover" /><div><h1 className="text-4xl font-bold text-text-primary">{user?.name}</h1><p className="text-text-secondary">{user?.email}</p></div></div>);} 