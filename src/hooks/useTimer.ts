import { useState, useEffect, useRef } from 'react';export function useTimer(initialSeconds: number) {const [seconds, setSeconds] = useState(initialSeconds);const [isActive, setIsActive] = useState(false);const timerRef = useRef<NodeJS.Timeout | null>(null);useEffect(() => {if (isActive && seconds > 0) {timerRef.current = setTimeout(() => {setSeconds(seconds - 1);}, 1000);} else if (seconds === 0) {setIsActive(false);}return () => {if (timerRef.current) clearTimeout(timerRef.current);};}, [isActive, seconds]);function start() {setIsActive(true);}function pause() {setIsActive(false);}function reset(newSeconds: number = initialSeconds) {setSeconds(newSeconds);setIsActive(false);}return {seconds, isActive, start, pause, reset};} 