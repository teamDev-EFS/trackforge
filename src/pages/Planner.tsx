import React, { useState } from 'react';import { useAppContext } from '../context/AppContext';import TaskList from '../components/planner/TaskList';import AddTaskModal from '../components/planner/AddTaskModal';import Button from '../components/ui/Button';import { Plus } from 'lucide-react';export default function Planner() {const { tasks, setTasks } = useAppContext();const [modalOpen, setModalOpen] = useState(false);function handleAddTask(task: typeof tasks[0]) {setTasks((prev) => [...prev, { ...task, order: prev.length + 1 }]);}return (<div className="space-y-6 animate-fadeIn"><h1 className="text-4xl font-bold tracking-tight">Planner</h1><div className="flex justify-end"><Button icon={Plus} onClick={() => setModalOpen(true)}>Add Task</Button></div><TaskList tasks={tasks} setTasks={setTasks} /><AddTaskModal isOpen={modalOpen} onClose={() => setModalOpen(false)} onAddTask={handleAddTask} /></div>);} 