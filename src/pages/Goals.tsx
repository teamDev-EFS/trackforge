import React, { useState } from 'react';import { useAppContext } from '../context/AppContext';import GoalList from '../components/goals/GoalList';import AddGoalModal from '../components/goals/AddGoalModal';import Button from '../components/ui/Button';import { Plus } from 'lucide-react';export default function Goals() {const { goals, setGoals } = useAppContext();const [modalOpen, setModalOpen] = useState(false);function handleAddGoal(goal: typeof goals[0]) {setGoals((prev) => [...prev, goal]);}return (<div className="space-y-6 animate-fadeIn"><h1 className="text-4xl font-bold tracking-tight">Goals</h1><div className="flex justify-end"><Button icon={Plus} onClick={() => setModalOpen(true)}>Add Goal</Button></div><GoalList goals={goals} setGoals={setGoals} /><AddGoalModal isOpen={modalOpen} onClose={() => setModalOpen(false)} onAddGoal={handleAddGoal} /></div>);} 