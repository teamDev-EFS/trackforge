import React, { useState } from 'react';import { useAppContext } from '../context/AppContext';import HabitList from '../components/habits/HabitList';import AddHabitModal from '../components/habits/AddHabitModal';import Button from '../components/ui/Button';import { Plus } from 'lucide-react';export default function Habits() {const { habits, setHabits } = useAppContext();const [modalOpen, setModalOpen] = useState(false);function handleAddHabit(habit: typeof habits[0]) {setHabits((prev) => [...prev, habit]);}return (<div className="space-y-6 animate-fadeIn"><h1 className="text-4xl font-bold tracking-tight">Habits</h1><div className="flex justify-end"><Button icon={Plus} onClick={() => setModalOpen(true)}>Add Habit</Button></div><HabitList habits={habits} setHabits={setHabits} /><AddHabitModal isOpen={modalOpen} onClose={() => setModalOpen(false)} onAddHabit={handleAddHabit} /></div>);} 