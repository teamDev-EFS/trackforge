import React, { useState } from 'react';import { useAppContext } from '../context/AppContext';import ReflectionForm from '../components/reflection/ReflectionForm';import ReflectionHistory from '../components/reflection/ReflectionHistory';import Button from '../components/ui/Button';import { Plus } from 'lucide-react';export default function Reflection() {const { reflections, setReflections } = useAppContext();const [modalOpen, setModalOpen] = useState(false);function handleAddReflection(reflection: typeof reflections[0]) {setReflections((prev) => [reflection, ...prev]);}return (<div className="space-y-6 animate-fadeIn"><h1 className="text-4xl font-bold tracking-tight">Daily Reflection</h1><div className="flex justify-end"><Button icon={Plus} onClick={() => setModalOpen(true)}>Add Reflection</Button></div><ReflectionHistory reflections={reflections} /><ReflectionForm isOpen={modalOpen} onClose={() => setModalOpen(false)} onAddReflection={handleAddReflection} /></div>);} 